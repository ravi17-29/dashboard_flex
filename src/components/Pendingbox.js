// src/components/PendingBox.js
import React from 'react';

const PendingBox = ({ selectedStatus }) => {
  // Logic to fetch and display pending employee data
  return (
    <div>
      <h3>Pending Box</h3>
      <p>List of employees with status: Pending</p>
      {/* Display the list of pending employees */}
    </div>
  );
};

export default PendingBox;
