// src/components/CompletedBox.js
import React from 'react';

const CompletedBox = ({ selectedStatus }) => {
  // Logic to fetch and display completed employee data
  return (
    <div>
      <h3>Completed Box</h3>
      <p>List of employees with status: Completed</p>
      {/* Display the list of completed employees */}
    </div>
  );
};

export default CompletedBox;
